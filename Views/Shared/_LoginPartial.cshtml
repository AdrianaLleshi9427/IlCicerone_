@using Microsoft.AspNet.Identity

<link href="~/Sailor/assets/css/login.css" rel="stylesheet" />

@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()

        <ul class="nav navbar-nav navbar-right" id="navbar-right">
            <!--<li>
                @Html.ActionLink("Hello " + (User.Identity.IsAuthenticated ? String.Concat(HttpContext.Current.User.Identity.Name.TakeWhile(c => c != '@')) : "Guest") + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })-->
                @*@(User.Identity.IsAuthenticated ? HttpContext.Current.User.Identity.Name : "Guest")*@

            <!--</li>-->
            <li>
                <a href="#" id="dropdown_login" class="dropdown-toggle" data-toggle="dropdown">Hello @String.Concat(HttpContext.Current.User.Identity.Name.TakeWhile(c => c != '@'))</a>
                    <ul class="dropdown-menu">
                        <li>@Html.ActionLink("Settings", "Index", "Dashboard", routeValues: null, htmlAttributes: new { title = "Manage" })</li>
                        <li>@Html.ActionLink("Profile", "Index", "Manage"/*, new { id = User.Identity.Name}*/)</li>
                        <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
                    </ul>
                </li>
            @*<li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>*@
        </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right" id="navbar-right">
        <li>@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
}
